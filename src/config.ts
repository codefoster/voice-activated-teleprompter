/**
 * Application configuration for infrequently changed settings
 */

export const appConfig = {
  // Brightness of the display content (0-100)
  brightness: 80,

  // Line position - vertical offset for where the active line appears on screen
  linePosition: 100,
};
